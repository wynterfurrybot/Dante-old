Set a moderation channel

Usage:
  set [type]

Example:
  set caselogs

Types:
  caselogs (Logs of moderation commands)
  messagelogs (Message edited, message deleted)
  userlogs (User joined, user left, user updated)
  otherlogs (Anything else)
